{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": ["**/.env*"],
  "ui": "stream",
  "tasks": {
    "topo": { "dependsOn": ["^topo"] },
    "build": {
      "env": [
        "APP_ENV",
        "BLOB_READ_WRITE_TOKEN",
        "DATABASE_URL",
        "AUTH_GOOGLE_ID",
        "AUTH_GOOGLE_SECRET",
        "RESEND_API_KEY",
        "FROM_EMAIL"
      ],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "dependsOn": ["^build"],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "next-env.d.ts",
        ".expo/**",
        "dist/**",
        "build/**",
        "lib/**"
      ]
    },
    "dev": {
      "env": [
        "APP_ENV",
        "BLOB_READ_WRITE_TOKEN",
        "DATABASE_URL",
        "AUTH_GOOGLE_ID",
        "AUTH_GOOGLE_SECRET",
        "RESEND_API_KEY",
        "FROM_EMAIL"
      ],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "cache": false,
      "persistent": true
    },
    "db:push": {
      "env": ["APP_ENV", "DATABASE_URL"],
      "inputs": ["drizzle.config.ts", "src/schema.ts", ".env*"],
      "cache": false
    },
    "db:gen": {
      "env": ["APP_ENV", "DATABASE_URL"],
      "inputs": ["drizzle.config.ts", "src/schema.ts", ".env*"],
      "outputs": ["drizzle/**"]
    },
    "db:std": {
      "env": ["APP_ENV", "DATABASE_URL"],
      "inputs": ["drizzle.config.ts", "src/schema.ts", ".env*"],
      "cache": false,
      "persistent": true
    },
    "clean": { "cache": false },
    "start": { "cache": false },
    "test": { "cache": false },
    "format": { "outputs": [] },
    "lint": { "outputs": [] },
    "typecheck": { "dependsOn": ["^topo"], "outputs": [] }
  }
}
