{
  "name": "mvp2",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "cross-env APP_ENV=${APP_ENV:-local} turbo build",
    "dev": "cross-env APP_ENV=${APP_ENV:-local} turbo dev --parallel",
    "clean": "git clean -xdf node_modules",
    "clean:workspaces": "turbo clean",
    "format": "biome format --write .",
    "lint": "turbo lint && manypkg check",
    "typecheck": "turbo typecheck",
    "test": "turbo test --parallel"
  },
  "packageManager": "bun@1.2.15",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@manypkg/cli": "^0.24.0",
    "dotenv": "^16.4.5",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
